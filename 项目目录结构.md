# GitTimePrism 项目目录结构

> 本文档记录 GitTimePrism 项目的完整目录结构，标注各文件的用途。
> 阶段 6（分支与提交操作）新增/修改的文件以 【阶段6】 标注。
> 阶段 7（远程操作增强 + 配置系统）新增/修改的文件以 【阶段7】 标注。
> 阶段 8（合并冲突解决 + Blame）新增/修改的文件以 【阶段8】 标注。
> 阶段 9（子模块 + LFS + GPG 签名 + difftool + 文件编码 + askpass）新增/修改的文件以 【阶段9】 标注。
> 阶段 10（仓库管理 + 状态持久化 + 头像）新增/修改的文件以 【阶段10】 标注。
> 阶段 11（文本格式化 + 键盘快捷键）新增/修改的文件以 【阶段11】 标注。
> 阶段 12（主框架集成 + i18n + 项目目录结构 + 集成测试）新增/修改的文件以 【阶段12】 标注。
> 阶段 13（验收 Gaps 修复：列头右键菜单 + 菜单可见性配置 + 对话框默认值 + committer 双套字段 + 旧代码清理）新增/修改的文件以 【阶段13】 标注。

## 项目根目录

```
GitTimePrism/
├── src-tauri/                    # Tauri 后端（Rust）
├── src/                          # 前端（TypeScript 原生 JS，无 React/Vue）
├── docs/                         # 参考文档与 gitgraph 源码
├── .trae/                        # Trae IDE 配置与 spec 文档
├── 项目目录结构.md                # 本文档
├── AGENTS.md                     # Agent 规范文档
├── package.json                  # Node.js 项目配置
├── tsconfig.json                 # TypeScript 配置
├── vite.config.js                # Vite 构建配置
└── index.html                    # 应用入口 HTML
```

## 后端目录结构（src-tauri/src/）

```
src-tauri/src/
├── main.rs                       # 程序入口
├── lib.rs                        # 库入口，注册所有 Tauri 命令 【阶段10扩展：注册 repo_manager/state/avatar 三个顶层模块 + 19 个新命令】
├── repo_manager.rs               # 【阶段10新增】仓库管理（discover_repos 递归搜索/register_repo/unregister_repo/ignore_repo/scan_submodules/export_config/import_config，配置文件 ~/.gittimeprism/repos.json）
├── state.rs                      # 【阶段10新增】状态持久化（get_repo_state/save_repo_state/get_global_state/save_global_state/touch_code_review，Code Review 90 天过期清理，配置文件 ~/.gittimeprism/state.json）
├── avatar.rs                     # 【阶段10新增】头像管理（get_avatar 异步获取 GitHub/GitLab/Gravatar 三源/clear_avatar_cache，14 天缓存刷新，identicon 4 天刷新，缓存目录 ~/.gittimeprism/avatars/）
├── askpass.rs                    # 【阶段9新增】askpass 凭证管理（set_credential/get_credential/clear_credential/has_credential/list_credential_hosts，OnceLock + Mutex 线程安全全局缓存，askpass helper 供 GIT_ASKPASS 调用）
│
├── git/                          # Git 操作核心模块
│   ├── mod.rs                    # 模块注册
│   ├── commands.rs               # 通用 Git 命令执行器（run_git/run_git_with_env/run_git_raw/GitError）
│   ├── repo.rs                   # 仓库管理（打开/初始化/克隆）
│   ├── log.rs                    # 提交日志查询
│   ├── graph.rs                  # 节点图数据组装（带 ref 注解的 AnnotatedCommit）【阶段13：Task 13.8 为旧 GraphCommit/CommitGraph/get_commit_graph 添加 #[deprecated] 属性 + #[allow(deprecated)] 抑制内部引用警告】
│   ├── refs.rs                   # 引用查询（heads/tags/remotes/HEAD）
│   ├── status.rs                 # 工作区状态（git status -z 解析）【阶段8扩展：detect_conflicts 检测合并冲突文件 + ConflictFile 结构体 + is_unmerged_pair 判断 UU/DU/UD/AU/UA/AA/DD 状态】
│   ├── diff.rs                   # 文件 diff（双命令组合：name-status + numstat）
│   ├── stage.rs                  # 暂存与提交（git add / git commit）
│   ├── branch.rs                 # 分支操作 【阶段6扩展：rename_branch/delete_branch/delete_remote_branch/checkout_commit/create_branch】
│   ├── checkout.rs               # 分支检出
│   ├── reset.rs                  # 重置操作 【阶段6扩展：reset_commit_to 支持任意 commit】
│   ├── tag.rs                    # 标签管理
│   ├── pull.rs                   # 拉取操作 【阶段6扩展：pull_with_options 支持 squash/no-ff/-S】
│   ├── push.rs                   # 推送操作 【阶段6扩展：push_with_options 支持 set-upstream/force/force-with-lease + push_tag】
│   ├── fetch.rs                  # 远程获取（fetch --all/--prune/--prune-tags）
│   ├── remote_mgmt.rs            # 远程仓库管理（add/delete/edit/prune/fetch_into_local_branch）
│   ├── stash.rs                  # Stash 操作（apply/pop/drop/push/branch_from_stash）
│   ├── commit_details.rs         # 提交详情（git show --format=...）
│   ├── commit_compare.rs         # 提交对比
│   ├── blame.rs                  # 【阶段8新增】Blame 操作（git blame --porcelain 解析，返回行级别 commit 信息：hash/author/email/date/line content）
│   ├── file_content.rs           # 文件内容读取（worktree/staged/HEAD/at_commit）【阶段8扩展：write_file_content 写入文件内容，供合并编辑器解决冲突后写回文件】
│   ├── file_ops.rs               # 【阶段7新增】文件操作（reset_file_to_revision/clean_untracked_files）
│   ├── archive.rs                # 【阶段7新增】归档（git archive）
│   ├── config.rs                 # 【阶段7新增】配置读取（git config --list -z）
│   ├── set_config.rs             # 【阶段7新增】配置修改（git config --local/--global）
│   ├── merge.rs                  # 【阶段6新增】合并操作（merge + squash 自动 commit）
│   ├── rebase.rs                 # 【阶段6新增】变基操作（rebase + 交互式标记）
│   ├── cherry_pick.rs            # 【阶段6新增】拣选操作（cherry-pick -x/-S/-m/--no-commit）
│   ├── revert.rs                 # 【阶段6新增】还原操作（revert --no-edit）
│   ├── drop_commit.rs            # 【阶段6新增】丢弃提交（rebase --onto + 拓扑检查）
│   ├── submodule.rs              # 【阶段9新增】子模块管理（读取 .gitmodules/add_submodule/update_submodules/delete_submodule）
│   ├── lfs.rs                    # 【阶段9新增】LFS 管理（lfs_install/lfs_track/lfs_untrack/lfs_list/lfs_locks/lfs_pull/lfs_push）
│   ├── tag_details.rs            # 【阶段9新增】标签详情（get_tag_details 执行 for-each-ref + verify-tag --raw，解析 [GNUPG:] 行 + TRUST_ 级别映射）
│   └── difftool.rs               # 【阶段9新增】外部 difftool（open_dir_diff 执行 git difftool --dir-diff）
│
├── commands/                     # Tauri 命令注册层（#[tauri::command]）
│   ├── mod.rs                    # 命令模块注册 【阶段10扩展：注册 repo_manager/state/avatar/watcher 四个新命令模块】
│   ├── repo.rs                   # 仓库命令（open_repo/init_repo/clone_repo）
│   ├── graph.rs                  # 节点图命令（get_commit_graph）【阶段13：Task 13.8 为旧 get_commit_graph 命令添加 #[allow(deprecated)] 抑制弃用警告】
│   ├── refs.rs                   # 引用查询命令（get_refs）
│   ├── stash.rs                  # Stash 命令（apply/pop/drop/push/branch_from_stash）
│   ├── stage.rs                  # 暂存命令（stage_file/stage_all/unstage_file/commit_changes）
│   ├── checkout.rs               # 检出命令 【阶段6扩展：rename_branch/delete_branch/delete_remote_branch/checkout_commit/create_branch】
│   ├── reset.rs                  # 重置命令 【阶段6重写：reset_commit 支持 commit 参数】
│   ├── tag.rs                    # 标签命令
│   ├── remote.rs                 # 远程命令 【阶段6扩展：pull_changes_with_options/push_changes_with_options/push_tag】
│   ├── fetch.rs                  # 获取命令
│   ├── remote_mgmt.rs            # 远程管理命令
│   ├── diff.rs                   # Diff 命令
│   ├── blame.rs                  # 【阶段8新增】Blame 命令（get_blame 调用 git::blame::get_blame）
│   ├── conflict.rs               # 【阶段8新增】冲突检测命令（detect_conflicts 调用 git::status::detect_conflicts）
│   ├── file_content.rs           # 文件内容命令 【阶段8扩展：write_file_content 命令，供合并编辑器写回文件】
│   ├── file_ops.rs               # 【阶段7新增】文件操作命令
│   ├── archive.rs                # 【阶段7新增】归档命令
│   ├── config.rs                 # 【阶段7新增】配置命令
│   ├── commit_details.rs         # 提交详情命令
│   ├── commit_compare.rs         # 提交对比命令
│   ├── merge.rs                  # 【阶段6新增】合并命令
│   ├── rebase.rs                 # 【阶段6新增】变基命令
│   ├── cherry_pick.rs            # 【阶段6新增】拣选命令
│   ├── revert.rs                 # 【阶段6新增】还原命令
│   ├── drop_commit.rs            # 【阶段6新增】丢弃提交命令
│   ├── repo_manager.rs           # 【阶段10新增】仓库管理命令（discover_repos/register_repo/unregister_repo/ignore_repo/list_registered_repos/scan_submodules/export_config/import_config）
│   ├── state.rs                  # 【阶段10新增】状态持久化命令（get_repo_state/save_repo_state/get_global_state/save_global_state/touch_code_review）
│   ├── avatar.rs                 # 【阶段10新增】头像管理命令（get_avatar 异步获取/clear_avatar_cache）
│   ├── watcher.rs                # 【阶段10新增】文件监听控制命令（start_watcher/stop_watcher/mute_watcher/unmute_watcher）
│   ├── submodule.rs              # 【阶段9新增】子模块命令（list_submodules/add_submodule/update_submodules/delete_submodule）
│   ├── lfs.rs                    # 【阶段9新增】LFS 命令（lfs_install/lfs_track/lfs_untrack/lfs_list/lfs_locks/lfs_pull/lfs_push）
│   ├── tag_details.rs            # 【阶段9新增】标签详情命令（get_tag_details）
│   ├── difftool.rs               # 【阶段9新增】difftool 命令（open_dir_diff）
│   ├── terminal.rs               # PTY 终端命令
│   ├── wallpaper.rs              # 壁纸命令
│   └── system.rs                 # 系统命令
│
└── utils/                        # 工具模块
    ├── mod.rs                    # 模块注册
    ├── process.rs                # 进程管理
    └── watcher.rs                # 文件监听 【阶段10重写：单仓库监听 + 750ms 防抖 + mute/unmute 机制 + FILE_CHANGE_REGEX 过滤 + 触发 repo_changed 事件】
```

## 前端目录结构（src/）

```
src/
├── main.ts                       # 应用入口
│
├── assets/                       # 静态资源目录
│   └── icons/
│       └── file-types/            # 【移植 vscode-icons】文件类型 SVG 图标资源（约 1567 个，包含 file_type_*.svg / folder_type_*.svg / folder_type_*_opened.svg / default_file.svg / default_folder.svg，由 docs/vscode-icons-master/icons/ 全量复制而来）
│
├── data/                         # 数据资源目录
│   └── vscode-icons-manifest.json # 【移植 vscode-icons】图标映射数据（files 920 项 + folders 178 项 + languages 407 项 + defaults 默认图标，从 vscode-icons-master 的 supportedExtensions.ts/supportedFolders.ts/languages.ts 抽取转换为纯 JSON，去除 VS Code/inversify 依赖）
│
├── components/                   # UI 组件
│   ├── app.ts                    # 主框架（工具栏/状态栏/键盘快捷键） 【阶段10扩展：initStateService 启动时恢复状态 + setupRepoChangedListener 监听 repo_changed 事件 + onRepoOpened 加载仓库状态 + 启动文件监听 + 注册仓库到后端】【阶段12扩展：导入 setMergeEditorCallbacks/setBlameViewerCallbacks + onRepoOpened 初始化合并编辑器和 Blame 视图回调 + handleFetch/pull/push 包裹 mute/unmute 文件监听 + 新增 checkConflictsAfterOperation 方法在冲突操作后自动检测并打开合并编辑器】
│   ├── commit-graph.ts           # 提交节点图（Canvas + HTML 表格） 【阶段10扩展：添加静态属性 avatarCache/imageResizer + fetchAvatarsForVisibleCommits/fetchAvatarForCell 异步获取作者头像 + buildRowHtml 在 author-cell 中加入头像 img 元素】【阶段13：Task 13.1 在 bindEvents 中为 thead th 绑定 contextmenu 事件，弹出列显隐菜单（日期列/作者列/提交列）】
│   ├── graph-canvas.ts           # Canvas 绘制核心（Vertex/Branch/Graph 算法）
│   ├── commit-detail.ts          # 提交详情面板（含对比模式 + Code Review）【阶段9扩展：Task 9.5 显示 GPG 签名状态（7 种状态图标+颜色+悬停详情）+ Task 9.6 文件右键菜单新增 Open in Diff Tool 选项】【阶段13：Task 13.4 新增 commitDetails 字段保存完整 GitCommitDetails + renderCommitHeader 显示 committer 双套字段（提交者/提交者邮箱/提交日期，仅当 committer 与 author 不同时显示）】
│   ├── commit-input.ts           # 提交消息输入框
│   ├── context-menu.ts           # 右键菜单组件
│   ├── context-menu-actions.ts   # 右键菜单动作生成器 【阶段6修改：所有菜单项改为 repoService 调用】【阶段12修改：runAction 通用执行器包裹 mute/unmute 文件监听 + 新增 checkConflictsAndOpenMergeEditor 函数在 merge/rebase/pull/cherry-pick/revert 操作后自动检测冲突并打开合并编辑器】【阶段13：Task 13.2 6 类菜单读取 contextMenuActionsVisibility 配置控制每项 visible + Task 13.3 各对话框从 configService 读取 dialog.* 默认值替代硬编码】
│   ├── dialog.ts                 # 通用对话框组件（5 种输入类型）
│   ├── diff-viewer.ts            # Diff 查看器
│   ├── file-list.ts              # 文件列表
│   ├── file-history.ts           # 文件历史
│   ├── find-widget.ts            # 搜索组件
│   ├── branch-list.ts            # 分支列表
│   ├── stash-manager.ts          # Stash 管理器
│   ├── tag-manager.ts            # 标签管理器
│   ├── merge-editor.ts           # 【阶段8新增】合并编辑器（三栏布局 Ours/Merged/Theirs，解析冲突标记，接受单段/全部，手动编辑，冲突导航，写回文件 + git add）
│   ├── blame-viewer.ts           # 【阶段8新增】Blame 视图（显示每行 commit hash/author/date，点击 hash 跳转提交详情，按 commit 分配颜色）
│   ├── settings-panel.ts         # 【阶段7新增】设置面板（完整配置 UI，12 个分组表单编辑）【阶段13：Task 13.2 新增"右键菜单可见性"分组（context-menu-visibility），渲染 6 类菜单共 40 个 visible 复选框，通过 data-key 属性自动收集】
│   ├── reset-dialog.ts           # 重置对话框
│   ├── git-installer.ts          # Git 安装引导
│   ├── submodule-manager.ts      # 【阶段9新增】子模块管理组件（SubmoduleManager 类，模态对话框，添加/更新/删除子模块）
│   ├── lfs-manager.ts            # 【阶段9新增】LFS 管理组件（LfsManager 类，模态对话框，LFS 初始化/跟踪规则/文件锁/对象拉推）
│   └── terminal.ts               # PTY 终端组件
│
├── services/                     # 服务层
│   ├── repo-service.ts           # 仓库服务 【阶段6扩展：merge/rebase/cherrypick/revert/dropCommit/renameBranch/deleteBranch/deleteRemoteBranch/checkoutCommit/createBranch/pullWithOptions/pushWithOptions/pushTag】【阶段8扩展：detectConflicts 检测合并冲突文件/getBlame 获取文件 Blame 信息/writeFileContent 写入文件内容】【阶段9扩展：listSubmodules/addSubmodule/updateSubmodules/deleteSubmodule 子模块 + lfsInstall/lfsTrack/lfsUntrack/lfsList/lfsLocks/lfsPull/lfsPush LFS + getTagDetails 标签详情 + openDirDiff difftool + getSupportedEncodings 文件编码 + setCredential/getCredential/clearCredential/hasCredential/listCredentialHosts 凭证管理】【阶段10扩展：getAvatar/clearAvatarCache 头像管理 + discoverRepos/registerRepo/unregisterRepo/ignoreRepo/listRegisteredRepos/scanSubmodules 仓库管理 + startWatcher/stopWatcher/muteWatcher/unmuteWatcher 文件监听控制】【阶段13：Task 13.8 为旧 GraphCommit/CommitGraph 接口和 getCommitGraphLegacy 方法添加 @deprecated JSDoc 标注】
│   ├── config-service.ts         # 【阶段7新增】配置服务（加载/修改/持久化配置）【阶段13：Task 13.2 新增 ContextMenuActionsVisibilityConfig 接口（6 类菜单每项 visible 开关）+ DEFAULT_APP_CONFIG 中 contextMenuActionsVisibility 默认值 + getContextMenuVisibilityConfig/getDialogDefault 辅助函数】
│   ├── state-service.ts          # 状态持久化服务 【阶段10扩展：loadStateFromBackend/saveStateToBackend/loadGlobalStateFromBackend/saveGlobalStateToBackend/touchCodeReview 通过 Tauri IPC 调用后端 state.rs + snake_case/camelCase 转换函数 + BackendRepoState/BackendGlobalState 后端类型定义】
│   ├── code-review-service.ts    # Code Review 服务
│   ├── theme-engine.ts           # 主题引擎
│   ├── wallpaper.ts              # 壁纸服务
│   ├── i18n.ts                   # 国际化
│   └── file-icon-service.ts      # 【文件图标服务新增】FileIconService 类（根据文件路径/文件夹名匹配 vscode-icons 图标，返回 SVG URL；内部用 import.meta.glob 静态导入 src/assets/icons/file-types/*.svg，结合 src/data/vscode-icons-manifest.json 构建 扩展名/文件名/语言扩展名/glob 查找表；导出单例 fileIconService；对外方法 getFileIconUrl(filePath)/getFolderIconUrl(folderName, isOpen)）
│
├── styles/                       # 样式文件
│   ├── global.css                # 全局样式
│   ├── variables.css             # CSS 变量定义
│   ├── components.css            # 组件样式（含按钮 hover 效果）
│   ├── context-menu.css          # 右键菜单样式
│   ├── dialog.css                # 对话框样式（含复选框/单选/按钮 hover）
│   ├── find-widget.css           # 搜索组件样式
│   ├── settings-widget.css       # 【阶段7新增】设置面板样式（模态对话框、表单、Issue Link 超链接）
│   ├── merge-editor.css          # 【阶段8新增】合并编辑器样式（模态浮层、三栏 Grid 布局、冲突块高亮 ours 蓝色/theirs 红色、已解决绿色、进度指示、响应式）
│   ├── blame-viewer.css          # 【阶段8新增】Blame 视图样式（模态浮层、行号 + commit 信息 + 代码内容三列布局、按 commit 分配颜色、边界提交样式、响应式）
│   ├── submodule-manager.css     # 【阶段9新增】子模块管理对话框样式（遮罩层、对话框容器、列表项、状态徽章、表单）
│   └── lfs-manager.css           # 【阶段9新增】LFS 管理对话框样式（遮罩层、对话框容器、跟踪规则列表、文件锁列表、表单）
│
├── utils/                        # 工具函数
│   ├── dom.ts                    # DOM 操作工具
│   ├── git-types.ts              # Git 类型定义
│   ├── git-utils.ts              # Git 工具函数
│   ├── graph-types.ts            # 图形类型定义
│   ├── text-formatter.ts         # 【阶段11重写】文本格式化器（完整 AST 解析：CommonMark emphasis/Emoji/Issue Linking/URL/Commit Hash）
│   ├── keyboard-shortcuts.ts     # 【阶段11新增】键盘快捷键系统（解析、匹配、冲突检测，支持 Ctrl/Shift/Alt 组合键）
│   └── pr-utils.ts               # 【阶段7新增】Pull Request 工具（Provider 检测、URL 生成）
│
└── i18n/                         # 国际化资源
    ├── zh-CN.json                # 中文 【阶段12重写：从 ~70 行扩展到 ~265 行，新增所有新功能 key（菜单项/对话框/工具栏/状态栏/错误消息/设置面板/Find Widget/合并编辑器/Blame 视图/子模块管理/LFS 管理）】
    └── en-US.json                # 英文 【阶段12重写：与 zh-CN.json 结构完全对应，英文翻译】
```

## 阶段 6 新增/修改文件汇总

### 后端新增文件（Rust）

| 文件路径 | 用途 |
|---------|------|
| `src-tauri/src/git/merge.rs` | 合并操作（支持 squash/no-ff/no-commit/-S，squash 后自动 commit） |
| `src-tauri/src/git/rebase.rs` | 变基操作（支持 ignore-date/-S/interactive 标记） |
| `src-tauri/src/git/cherry_pick.rs` | 拣选操作（支持 --no-commit/-x/-S/-m） |
| `src-tauri/src/git/revert.rs` | 还原操作（固定 --no-edit，支持 -S/-m） |
| `src-tauri/src/git/drop_commit.rs` | 丢弃提交（rebase --onto + is_ancestor 拓扑检查） |
| `src-tauri/src/commands/merge.rs` | 合并命令注册 |
| `src-tauri/src/commands/rebase.rs` | 变基命令注册 |
| `src-tauri/src/commands/cherry_pick.rs` | 拣选命令注册 |
| `src-tauri/src/commands/revert.rs` | 还原命令注册 |
| `src-tauri/src/commands/drop_commit.rs` | 丢弃提交命令注册 |

### 后端修改文件（Rust）

| 文件路径 | 修改内容 |
|---------|---------|
| `src-tauri/src/git/branch.rs` | 新增 rename_branch/delete_branch/delete_remote_branch/checkout_commit/create_branch 方法 |
| `src-tauri/src/git/reset.rs` | 新增 reset_commit_to 函数支持 reset 到任意 commit |
| `src-tauri/src/git/pull.rs` | 新增 pull_with_options 方法支持 squash/no-ff/-S |
| `src-tauri/src/git/push.rs` | 新增 push_with_options 和 push_tag 方法 |
| `src-tauri/src/git/mod.rs` | 注册 merge/rebase/cherry_pick/revert/drop_commit 模块 |
| `src-tauri/src/commands/checkout.rs` | 新增 rename_branch/delete_branch/delete_remote_branch/checkout_commit/create_branch 命令 |
| `src-tauri/src/commands/reset.rs` | 重写 reset_commit 命令支持 commit 参数 |
| `src-tauri/src/commands/remote.rs` | 新增 pull_changes_with_options/push_changes_with_options/push_tag 命令 |
| `src-tauri/src/commands/mod.rs` | 注册 merge/rebase/cherry_pick/revert/drop_commit 命令模块 |
| `src-tauri/src/lib.rs` | 在 invoke_handler 中注册所有新命令 |

### 前端修改文件（TypeScript）

| 文件路径 | 修改内容 |
|---------|---------|
| `src/services/repo-service.ts` | 新增 merge/rebase/cherrypick/revert/dropCommit/renameBranch/deleteBranch/deleteRemoteBranch/checkoutCommit/createBranch/pullWithOptions/pushWithOptions/pushTag 方法；扩展 resetCommit 支持可选 commit 参数 |
| `src/components/context-menu-actions.ts` | 将所有菜单项从直接 invoke() 改为 repoService 方法调用（提交菜单/本地分支菜单/远程分支菜单/标签菜单/Stash菜单/未提交变更菜单） |

## 阶段 7 新增/修改文件汇总

### 后端新增文件（Rust）

| 文件路径 | 用途 |
|---------|------|
| `src-tauri/src/git/config.rs` | 配置读取（执行 `git config --list -z --includes`，解析 branch.*/remote.*/user.*/push.default） |
| `src-tauri/src/git/set_config.rs` | 配置修改（set_config_value/unset_config_value，支持 --local/--global） |
| `src-tauri/src/git/file_ops.rs` | 文件操作（reset_file_to_revision 执行 `git checkout {hash} -- {file}`，clean_untracked_files 执行 `git clean -f[d]`） |
| `src-tauri/src/git/archive.rs` | 归档操作（执行 `git archive` 导出 zip/tar 等格式） |
| `src-tauri/src/commands/config.rs` | 配置命令注册（get_config/set_config/unset_config） |
| `src-tauri/src/commands/file_ops.rs` | 文件操作命令注册（reset_file_to_revision/clean_untracked_files） |
| `src-tauri/src/commands/archive.rs` | 归档命令注册（archive） |

### 后端修改文件（Rust）

| 文件路径 | 修改内容 |
|---------|---------|
| `src-tauri/src/git/mod.rs` | 注册 config/set_config/file_ops/archive 模块 |
| `src-tauri/src/commands/mod.rs` | 注册 config/file_ops/archive 命令模块 |
| `src-tauri/src/lib.rs` | 在 invoke_handler 中注册所有新命令（get_config/set_config/unset_config/reset_file_to_revision/clean_untracked_files/archive） |
| `src-tauri/src/commands/system.rs` | 新增 open_external_url 命令（在默认浏览器中打开 URL，供 Issue Link 超链接和 PR Creation 使用） |

### 前端新增文件（TypeScript）

| 文件路径 | 用途 |
|---------|------|
| `src/components/settings-panel.ts` | 设置面板组件（完整配置 UI，12 个分组表单编辑：Commit Details/Date/Dialog/Graph/Keyboard/Repository/Reference Labels/Encoding/Markdown/Accessibility/Issue Linking/Git User，含保存/重置按钮） |
| `src/services/config-service.ts` | 配置服务（加载仓库配置 invoke('get_config')，修改配置 invoke('set_config')，持久化前端配置到 localStorage，深度合并默认值） |
| `src/utils/text-formatter.ts` | 文本格式化器（Issue Linking：commit message 中 `#123` 转超链接，支持 `$1`/`$2` 占位符替换，HTML 转义） |
| `src/utils/pr-utils.ts` | Pull Request 工具（detectPullRequestProvider 根据 remote URL 匹配 github/gitlab/bitbucket，parseRemoteUrl 解析 HTTPS/SSH URL，generatePullRequestUrl 生成 PR 创建 URL，createPullRequest 调用 open_external_url） |
| `src/styles/settings-widget.css` | 设置面板样式（模态对话框、毛玻璃遮罩、左侧导航 + 右侧内容布局、表单控件、Issue Link 超链接样式） |

### 前端修改文件（TypeScript）

| 文件路径 | 修改内容 |
|---------|---------|
| `src/components/app.ts` | 导入 SettingsPanel 组件；工具栏新增 Settings 按钮（在 Find 和 Wallpaper 按钮之间）；添加 Settings 按钮点击事件监听器（打开设置面板，保存后刷新所有组件） |
| `src/components/commit-graph.ts` | 导入 formatCommitMessage；commit message 渲染从 escapeHtml 改为 formatCommitMessage（支持 Issue Linking）；在 bindRowEvents 中为 a.externalUrl 添加点击事件监听器（阻止冒泡，调用 open_external_url 打开链接） |
| `src/components/context-menu-actions.ts` | 导入 createPullRequest/buildPullRequestConfigFromRemote 和 configService；重写本地分支菜单和远程分支菜单的 Create Pull Request 实现（从远程 URL 自动构建 PR 配置，调用 createPullRequest 打开预填表单） |
| `src/main.ts` | 引入 settings-widget.css 样式文件 |

## 阶段 8 新增/修改文件汇总

### 后端新增文件（Rust）

| 文件路径 | 用途 |
|---------|------|
| `src-tauri/src/git/blame.rs` | Blame 操作（执行 `git blame --porcelain`，解析行级别 commit 信息；含 BlameLine 结构体 11 个字段、unix_to_iso8601 Unix 时间戳转 ISO 8601 UTC、get_blame 主入口、parse_blame_output 状态机解析器） |
| `src-tauri/src/commands/blame.rs` | Blame 命令注册（get_blame 命令封装，调用 git::blame::get_blame） |
| `src-tauri/src/commands/conflict.rs` | 冲突检测命令注册（detect_conflicts 命令封装，调用 git::status::detect_conflicts） |

### 后端修改文件（Rust）

| 文件路径 | 修改内容 |
|---------|---------|
| `src-tauri/src/git/status.rs` | 新增 ConflictFile 结构体（path/ours_hash/theirs_hash/base_hash 四字段，对应 git ls-files -u 的 stage 1/2/3）；新增 is_unmerged_pair 函数判断 unmerged 状态码组合（DD/AU/UD/UA/DU/AA/UU）；新增 detect_conflicts 函数执行 `git ls-files -u -z` 并按路径分组返回 Vec<ConflictFile> |
| `src-tauri/src/git/file_content.rs` | 新增 write_file_content 函数（使用 std::fs::write 写入文件，覆盖原有内容；支持自动创建父目录；用于合并编辑器解决冲突后写回文件） |
| `src-tauri/src/commands/file_content.rs` | 新增 write_file_content 命令封装（接收 repo_path/file_path/content 三个参数，调用 git::file_content::write_file_content） |
| `src-tauri/src/git/mod.rs` | 注册 blame 模块（pub mod blame;） |
| `src-tauri/src/commands/mod.rs` | 注册 blame 和 conflict 命令模块（pub mod blame; pub mod conflict;） |
| `src-tauri/src/lib.rs` | 在 invoke_handler! 中注册新命令：commands::blame::get_blame、commands::conflict::detect_conflicts、commands::file_content::write_file_content |

### 前端新增文件（TypeScript）

| 文件路径 | 用途 |
|---------|------|
| `src/components/merge-editor.ts` | 合并编辑器组件（三栏布局 Ours/Merged/Theirs；parseConflicts 状态机解析 `<<<<<<<`/`=======`/`>>>>>>>` 冲突标记；acceptAllOurs/acceptAllTheirs 一键接受；goToPrevConflict/goToNextConflict 冲突导航；saveAndMarkResolved 写回文件 + git add；进度指示 X/Y 冲突已解决；ESC 关闭） |
| `src/components/blame-viewer.ts` | Blame 视图组件（COMMIT_COLORS 8 种颜色调色板循环分配；open 方法创建模态视图；renderBlameLines 使用 DocumentFragment 批量渲染；按 commit hash 分配颜色（hashColorMap 维护映射）；点击 hash 跳转提交详情；formatDate ISO 8601 转本地时间；ESC 关闭；refresh 刷新） |
| `src/styles/merge-editor.css` | 合并编辑器样式（模态遮罩层 z-index 10000；主容器 90vw×90vh 圆角阴影滑入动画；工具栏含接受所有 ours/theirs、上一个/下一个、保存按钮；三栏 CSS Grid 1fr 1fr 1fr；冲突块高亮 ours 蓝色调 #0066cc/theirs 红色调 #cc0000；当前冲突块橙色高亮；已解决绿色 + 透明度 0.5；响应式 <768px 单列布局） |
| `src/styles/blame-viewer.css` | Blame 视图样式（模态遮罩层；主容器；标题栏含文件名 + 关闭按钮；工具栏含刷新按钮；单行 CSS Grid 50px 280px 1fr 行号 | commit 信息 | 代码内容；行号列灰色背景右对齐；commit 信息列 hash 蓝色可点击 + author + date；边界提交样式 .blame-line.boundary 更浅色调；代码内容列保留空白水平滚动；响应式 <768px 隐藏 commit 信息列） |

### 前端修改文件（TypeScript）

| 文件路径 | 修改内容 |
|---------|---------|
| `src/services/repo-service.ts` | 新增 ConflictFile 接口（path/ours_hash/theirs_hash/base_hash，对应 Rust 结构体）；新增 BlameLine 接口（11 个字段：commit_hash/short_hash/author/author_email/author_date/committer/committer_email/committer_date/line_number/line_content/is_boundary）；新增三个方法：detectConflicts（调用 detect_conflicts 命令）、getBlame（调用 get_blame 命令）、writeFileContent（调用 write_file_content 命令） |
| `src/components/commit-detail.ts` | 在文件右键菜单 actions 数组中，在 "View Diff with Working File" 和 "Open File" 之间新增 "View Blame" 菜单项（visible 条件为 fileExistsAtThisRevision）；新增 viewBlame 私有方法（使用动态 import 避免循环依赖，调用 blameViewer.open 打开 Blame 视图） |
| `src/main.ts` | 在 import './styles/settings-widget.css'; 后新增两行 CSS 导入：import './styles/merge-editor.css'; 和 import './styles/blame-viewer.css'; |

## 阶段 9 新增/修改文件汇总

### 后端新增文件（Rust）

| 文件路径 | 用途 |
|---------|------|
| `src-tauri/src/git/submodule.rs` | 子模块管理（读取 .gitmodules 解析子模块列表、add_submodule 添加子模块、update_submodules 更新子模块、delete_submodule 删除子模块） |
| `src-tauri/src/git/lfs.rs` | LFS 管理（lfs_install 初始化 LFS、lfs_track 添加跟踪规则、lfs_untrack 移除跟踪规则、lfs_list 列出跟踪规则、lfs_locks 查看文件锁、lfs_pull 拉取 LFS 对象、lfs_push 推送 LFS 对象） |
| `src-tauri/src/git/tag_details.rs` | 标签详情（get_tag_details 执行 git for-each-ref 获取标签元信息 + git verify-tag --raw 验证 GPG 签名，解析 [GNUPG:] 行 + TRUST_ 级别映射） |
| `src-tauri/src/git/difftool.rs` | 外部 difftool（open_dir_diff 执行 git difftool --dir-diff 在外部对比工具中打开目录对比） |
| `src-tauri/src/askpass.rs` | askpass 凭证管理（set_credential/get_credential/clear_credential/has_credential/list_credential_hosts，OnceLock + Mutex 线程安全全局缓存，askpass helper 二进制供 GIT_ASKPASS 环境变量调用） |
| `src-tauri/src/commands/submodule.rs` | 子模块命令注册（list_submodules/add_submodule/update_submodules/delete_submodule） |
| `src-tauri/src/commands/lfs.rs` | LFS 命令注册（lfs_install/lfs_track/lfs_untrack/lfs_list/lfs_locks/lfs_pull/lfs_push） |
| `src-tauri/src/commands/tag_details.rs` | 标签详情命令注册（get_tag_details） |
| `src-tauri/src/commands/difftool.rs` | difftool 命令注册（open_dir_diff） |

### 后端修改文件（Rust）

| 文件路径 | 修改内容 |
|---------|---------|
| `src-tauri/src/git/file_content.rs` | 新增 encoding_rs 多编码解码支持（UTF-8/GBK/Big5/Shift-JIS/Windows-1252/EUC-KR/UTF-16），新增 get_supported_encodings 命令返回支持的编码列表 |
| `src-tauri/src/git/mod.rs` | 注册 submodule/lfs/tag_details/difftool 模块 |
| `src-tauri/src/commands/mod.rs` | 注册 submodule/lfs/tag_details/difftool 命令模块 |
| `src-tauri/src/lib.rs` | 注册 askpass 顶层模块 + 在 invoke_handler 中注册所有阶段 9 新命令 |
| `src-tauri/Cargo.toml` | 添加 encoding_rs 依赖 |
| `src-tauri/src/utils/watcher.rs` | 修复块注释中 glob 通配符导致的编译错误 |

### 前端新增文件（TypeScript）

| 文件路径 | 用途 |
|---------|------|
| `src/components/submodule-manager.ts` | 子模块管理组件（SubmoduleManager 类，模态对话框形式，支持添加/更新/删除子模块，显示 path/URL/当前提交/差异状态） |
| `src/components/lfs-manager.ts` | LFS 管理组件（LfsManager 类，模态对话框形式，支持 LFS 初始化/跟踪规则管理/文件锁查看/对象拉推） |
| `src/styles/submodule-manager.css` | 子模块管理对话框样式（遮罩层、对话框容器、列表项、状态徽章、表单） |
| `src/styles/lfs-manager.css` | LFS 管理对话框样式（遮罩层、对话框容器、跟踪规则列表、文件锁列表、表单） |

### 前端修改文件（TypeScript）

| 文件路径 | 修改内容 |
|---------|---------|
| `src/services/repo-service.ts` | 新增类型定义（SubmoduleInfo/LfsPattern/LfsLock/SignatureStatus 普通 enum/CommitSignature/TagDetails/Credential）；新增 19 个方法：listSubmodules/addSubmodule/updateSubmodules/deleteSubmodule（子模块）、lfsInstall/lfsTrack/lfsUntrack/lfsList/lfsLocks/lfsPull/lfsPush（LFS）、getTagDetails（标签详情）、openDirDiff（difftool）、getSupportedEncodings（文件编码）、setCredential/getCredential/clearCredential/hasCredential/listCredentialHosts（凭证） |
| `src/components/commit-detail.ts` | 阶段 9：Task 9.5：新增 commitSignature 字段保存 GPG 签名信息；在 showCommit 中调用 getCommitDetails 获取签名信息（hasParents 重试机制处理初始提交）；在 renderCommitHeader 中显示签名状态行（图标+文字+颜色，悬停显示签名者和密钥 ID）；新增 getSignatureDisplay 方法映射 7 种签名状态到图标/文字/CSS 类名。阶段 9：Task 9.6：在文件右键菜单第一组新增 "Open in Diff Tool" 选项（调用 openDirDiff 在外部 difftool 中打开目录对比）；新增 openInDiffTool 方法 |
| `src/components/app.ts` | 导入 SubmoduleManager 和 LfsManager 组件；在工具栏新增"📦 子模块"和"🗃 LFS"按钮；添加按钮点击事件处理（打开对应管理对话框） |
| `src/main.ts` | 新增 CSS 导入：import './styles/submodule-manager.css'; 和 import './styles/lfs-manager.css'; |
| `src/styles/components.css` | 新增 GPG 签名状态样式（.commit-signature-valid/unknown/expired/revoked/unchecked/bad，6 种颜色区分签名状态） |

## 阶段 11 新增/修改文件汇总

### 后端修改文件（Rust）

| 文件路径 | 修改内容 |
|---------|---------|
| `src-tauri/src/commands/system.rs` | 新增 get_git_version 命令（调用 git::commands::git_version() 获取 Git 版本号，用于状态栏显示） |
| `src-tauri/src/lib.rs` | 在 invoke_handler! 中注册新命令：commands::system::get_git_version |

### 前端新增文件（TypeScript）

| 文件路径 | 用途 |
|---------|------|
| `src/utils/keyboard-shortcuts.ts` | 键盘快捷键系统（ShortcutAction 类型 14 种动作、KeyboardShortcut 接口含 ctrl/alt/shift/key 字段、DEFAULT_SHORTCUTS 默认映射、parseShortcutString 解析字符串为结构化表示、matchShortcut 匹配 KeyboardEvent 与快捷键、detectConflicts 检测冲突、buildShortcutMap 构建完整映射表；macOS 上 Ctrl 自动映射为 Cmd 键） |

### 前端修改文件（TypeScript）

| 文件路径 | 修改内容 |
|---------|---------|
| `src/utils/text-formatter.ts` | 从简化版（仅 Issue Linking）重写为完整 AST 解析版本：新增 NodeType enum（10 种节点类型）、EmphasisDelimiterType enum、TextFormatter 类（含 CommonMark left-flanking/right-flanking 算法）、EMOJI_MAPPINGS（约 60 个 emoji）、registerCustomEmojiMappings 自定义函数、formatLine 主入口、insertIntoTreeIfNoOverlap 拒绝重叠节点；解析顺序：Code → URL → Issue Link → Commit Hash → Backslash Escape → Emoji → Emphasis；使用普通 enum（非 const enum）兼容 isolatedModules |
| `src/components/commit-graph.ts` | 集成 TextFormatter：bindRowEvents 方法新增外部 URL（a.externalUrl）点击事件（调用 invoke('open_external_url')）和内部 URL（span.internalUrl[data-type="commit"]）点击事件（触发 onCommitSelect 回调） |
| `src/components/commit-detail.ts` | 集成 TextFormatter：renderCommitHeader 方法使用 formatLine 格式化标题和正文（从 configService 读取 markdown/emoji/issueLinking 配置）；新增 bindUrlClickEvents 方法处理外部 URL 点击事件；将 `<pre>` 标签改为 `<div>` 标签以支持 HTML 格式化内容 |
| `src/services/config-service.ts` | 扩展 KeyboardShortcutsConfig 接口（从 4 个字段扩展到 14 个：新增 scrollToPrevStash/navigateUp/navigateDown/navigateSameBranchUp/navigateSameBranchDown/navigateAltBranchUp/navigateAltBranchDown/commitDialog/closeOverlay/toggleTerminal）；更新 DEFAULT_APP_CONFIG 中的 keyboardShortcuts 默认值（从单字符格式如 'f' 改为完整快捷键字符串如 'Ctrl+F'） |
| `src/components/app.ts` | 重写 initKeyboardShortcuts 方法（使用 parseShortcutString + matchShortcut 替代硬编码判断，支持 14 种快捷键动作，输入框中单键不触发）；新增 navigateCommit 方法（Up/Down 切换上/下提交）；新增 updateGitVersion 方法（调用 get_git_version 命令更新状态栏 Git 版本）；新增 updateStatusbarBranch 方法（更新分支名和 ahead/behind 显示）；新增 updateStatusbarCommitInfo 方法（更新提交总数和排序方式显示）；修改状态栏 HTML 结构（新增分支/ahead-behind/提交总数/排序方式显示项和分隔符）；在 refreshAllComponents 中调用状态栏更新方法 |
| `src/components/settings-panel.ts` | 重写 renderKeyboardSection 方法（从 4 个快捷键配置项扩展到 14 个，支持完整快捷键字符串格式如 "Ctrl+Shift+S"、"Up"、"Enter"，移除 maxlength="1" 限制） |
| `src/components/branch-list.ts` | 新增 getCurrentBranchAheadBehind 方法（返回当前分支的 ahead/behind 计数，用于状态栏显示） |
| `src/styles/components.css` | 新增状态栏样式：.statusbar-separator（分隔符）、.statusbar-icon（图标）、#statusbar-branch-name（分支名高亮）、#statusbar-ahead-behind-text（ahead/behind）、#statusbar-commit-count-text（提交总数）、#statusbar-sort-order-text（排序方式斜体） |

## 阶段 10 新增/修改文件汇总

### 后端新增文件（Rust）

| 文件路径 | 用途 |
|---------|------|
| `src-tauri/src/repo_manager.rs` | 仓库管理模块（discover_repos 递归搜索含 .git 的文件夹，跳过 node_modules/target/.git 等；register_repo/unregister_repo/ignore_repo 维护 ~/.gittimeprism/repos.json；scan_submodules 调用 `git submodule status`；export_config/import_config 导入导出 .gittimeprism.json） |
| `src-tauri/src/state.rs` | 状态持久化模块（RepoState/GlobalState/ColumnWidths/FindWidgetState/CodeReviewState 结构体；get_repo_state/save_repo_state/get_global_state/save_global_state/touch_code_review；Code Review 90 天过期清理 cleanup_expired_code_reviews；持久化到 ~/.gittimeprism/state.json） |
| `src-tauri/src/avatar.rs` | 头像管理模块（AvatarCacheEntry 缓存条目；compute_email_md5 计算 MD5 哈希；detect_remote_source 检测 GitHub/GitLab/Gravatar 源；fetch_from_gravatar 先尝试 d=404 再 d=identicon；get_avatar 异步获取并缓存到 ~/.gittimeprism/avatars/；14 天缓存刷新，identicon 4 天刷新） |
| `src-tauri/src/commands/repo_manager.rs` | 仓库管理命令注册（8 个命令：discover_repos/register_repo/unregister_repo/ignore_repo/list_registered_repos/scan_submodules/export_config/import_config） |
| `src-tauri/src/commands/state.rs` | 状态持久化命令注册（5 个命令：get_repo_state/save_repo_state/get_global_state/save_global_state/touch_code_review） |
| `src-tauri/src/commands/avatar.rs` | 头像管理命令注册（2 个命令：get_avatar 异步/clear_avatar_cache） |
| `src-tauri/src/commands/watcher.rs` | 文件监听控制命令注册（4 个命令：start_watcher/stop_watcher/mute_watcher/unmute_watcher） |

### 后端修改文件（Rust）

| 文件路径 | 修改内容 |
|---------|---------|
| `src-tauri/src/utils/watcher.rs` | 重写为单仓库监听 + 750ms 防抖（DEBOUNCE_DELAY_MS=750）+ mute/unmute 机制（UNMUTE_IGNORE_WINDOW_MS=1500）+ FILE_CHANGE_REGEX 手动实现 matches_file_change 过滤（.git/config/.git/index/.git/HEAD/.git/refs/stash/.git/refs/heads/*/.git/refs/remotes/*/.git/refs/tags/*）+ 触发 repo_changed Tauri 事件 |
| `src-tauri/src/commands/mod.rs` | 注册新命令模块：repo_manager/state/avatar/watcher |
| `src-tauri/src/lib.rs` | 注册顶层模块（pub mod repo_manager/state/avatar）+ 在 invoke_handler! 中注册 19 个新命令 |
| `src-tauri/Cargo.toml` | 添加依赖：notify=6（文件监听）、reqwest={0.12, features=["json","blocking"]}（HTTP 客户端）、tokio={1, features=["full"]}（异步运行时）、md-5=0.10（MD5 哈希）、dirs=5（用户目录）、chrono={0.4, features=["serde"]}（时间戳）、hex=0.4（十六进制转换） |

### 前端修改文件（TypeScript）

| 文件路径 | 修改内容 |
|---------|---------|
| `src/components/commit-graph.ts` | 添加 ImageResizer 导入；新增静态属性 avatarCache（Map<string, string \| null>）和 imageResizer（ImageResizer 实例）跨实例共享头像缓存；新增 fetchAvatarsForVisibleCommits 方法批量获取可见提交的头像；新增 fetchAvatarForCell 方法单个获取并设置 img.src（使用 convertFileSrc 转换本地路径 + ImageResizer.resize 缩放到 18×18 + onload 淡入显示）；修改 buildRowHtml 在 author-cell 中加入头像占位 img 元素（opacity:0 占位，加载完成后淡入）；在 render() 末尾调用 fetchAvatarsForVisibleCommits() |
| `src/components/app.ts` | 导入 stateService 和 listen（@tauri-apps/api/event）；新增 unlistenRepoChanged 字段（取消监听函数）；新增 initStateService 方法（启动时从后端加载全局状态到 localStorage）；新增 setupRepoChangedListener 方法（监听 repo_changed 事件触发 refreshAllComponents）；在 init() 中调用这两个方法；在 onRepoOpened 中调用 stateService.loadStateFromBackend 恢复仓库状态、调用 repoService.registerRepo 注册仓库、调用 repoService.startWatcher 启动文件监听 |
| `src/services/state-service.ts` | 导入 invoke；新增 BackendRepoState/BackendGlobalState 接口（与 Rust 后端 snake_case 字段对应）；新增 convertBackendStateToFrontend/convertFrontendStateToBackend 转换函数（snake_case ↔ camelCase）；新增 loadStateFromBackend/saveStateToBackend/loadGlobalStateFromBackend/saveGlobalStateToBackend/touchCodeReview 方法（通过 Tauri IPC 调用后端 state.rs 命令） |
| `src/services/repo-service.ts` | 新增 RepoEntry 接口（path/name/is_registered/last_opened/has_submodules）；新增 12 个方法：getAvatar/clearAvatarCache（头像管理）、discoverRepos/registerRepo/unregisterRepo/ignoreRepo/listRegisteredRepos/scanSubmodules（仓库管理）、startWatcher/stopWatcher/muteWatcher/unmuteWatcher（文件监听控制） |

## 阶段 12 新增/修改文件汇总

### 阶段 12 概述

阶段 12 是最终集成阶段，主要工作是主框架集成、i18n 完整化、项目目录结构文档更新和集成测试（代码审查方式）。本阶段不新增文件，仅修改现有文件以完成所有集成点。

### 前端修改文件（TypeScript）

| 文件路径 | 修改内容 |
|---------|---------|
| `src/components/app.ts` | **主框架集成**：1) 导入 setMergeEditorCallbacks（来自 merge-editor.js）和 setBlameViewerCallbacks（来自 blame-viewer.js）回调设置函数；2) 在 onRepoOpened 方法中调用 setMergeEditorCallbacks 注入关闭回调和保存成功回调（均为 refreshAllComponents），调用 setBlameViewerCallbacks 注入提交点击回调（showCommitDetailByHash）和关闭回调；3) 修改 handleFetch 方法，在 fetch 操作前后调用 muteWatcher/unmuteWatcher 包裹，避免自身操作触发 repo_changed 事件导致循环刷新；4) 修改 pull 按钮回调，添加 mute/unmute 包裹和冲突检测；5) 修改 push 按钮回调，添加 mute/unmute 包裹；6) 新增 checkConflictsAfterOperation 私有方法：调用 repoService.detectConflicts 检测冲突文件，若存在则动态 import merge-editor 单例并打开第一个冲突文件，返回是否检测到冲突的布尔值 |
| `src/components/context-menu-actions.ts` | **右键菜单动作集成**：1) 重写 runAction 通用执行器，在所有 Git 操作前后包裹 muteWatcher/unmuteWatcher（操作前静音文件监听器避免自身操作触发刷新，操作后取消静音恢复正常监听，try-catch 确保即使 mute/unmute 失败也不影响主流程）；2) 新增 checkConflictsAndOpenMergeEditor 异步函数：检测 actionName 是否为可能产生冲突的操作（合并/变基/拉取/拣选/还原/merge/rebase/pull/cherry/revert），若是则调用 repoService.detectConflicts 检测冲突文件，检测到冲突时动态 import merge-editor 单例并打开第一个冲突文件，返回是否检测到冲突的布尔值；3) 在 runAction 的成功路径和失败路径都调用 checkConflictsAndOpenMergeEditor（操作失败可能是因为产生了冲突），若检测到冲突则跳过错误对话框显示 |
| `src/i18n/zh-CN.json` | **i18n 完整重写**：从原来的 ~70 行扩展到 ~265 行，新增以下所有新功能 key 分组：toolbar（find/settings/stash/submodules/lfs/tagManager/resetCommit/wallpaper/refresh）、sidebar（fileChanges/commit）、detail（codeCompare）、statusbar（branch/commitCount/sortOrder/repoPath/ahead/behind/sortByDate/sortByAuthorDate/sortByTopo）、common（apply/rename/add/yes/no/loading/error/success/warning）、menu（30+ 菜单项：）、dialog（所有对话框按钮和标题）、settings（所有设置面板分组名称）、find（搜索/大小写敏感/正则/上一个/下一个/无匹配/占位符）、mergeEditor（ours/merged/theirs/acceptAllOurs/acceptAllTheirs/previousConflict/nextConflict/resolved/save/conflictsResolved/title/close）、blame（title/lineNumber/author/date/commit/refresh/close）、submodule（所有子模块管理 key）、lfs（所有 LFS 管理 key）、error（所有错误消息）、success（所有成功消息） |
| `src/i18n/en-US.json` | **i18n 完整重写**：与 zh-CN.json 结构完全对应，英文翻译。新增所有新功能 key（菜单项/对话框/工具栏/状态栏/错误消息/设置面板/Find Widget/合并编辑器/Blame 视图/子模块管理/LFS 管理） |

### 阶段 12 集成测试验证（代码审查方式）

| 检查项 | 结果 |
|-------|------|
| 类型一致性：repo-service.ts 方法参数和返回类型与后端 Rust 命令签名匹配 | ✅ 通过（detectConflicts 返回 ConflictFile[]、getBlame 返回 BlameLine[]、muteWatcher/unmuteWatcher 返回 void） |
| 命令注册：lib.rs 中确认所有新命令已注册到 invoke_handler! | ✅ 通过（system/terminal/repo/stage/diff/graph/checkout/reset/tag/remote/file_content/refs/stash/commit_details/commit_compare/fetch/remote_mgmt/config/file_ops/archive/merge/rebase/cherry_pick/revert/drop_commit/conflict/blame/submodule/lfs/tag_details/difftool/askpass/repo_manager/state/avatar/watcher 全部注册） |
| CSS 导入：main.ts 中确认所有新增 CSS 文件已 import | ✅ 通过（variables/global/components/context-menu/dialog/find-widget/settings-widget/merge-editor/blame-viewer/submodule-manager/lfs-manager 共 11 个 CSS 文件均已导入） |
| 关键集成点：app.ts 组件初始化 | ✅ 通过（setMergeEditorCallbacks/setBlameViewerCallbacks 在 onRepoOpened 中调用） |
| 关键集成点：commit-graph 回调连接 | ✅ 通过（setOnContextMenu 回调已正确连接到 app.ts 的 handleContextMenu） |
| 关键集成点：文件监听 mute/unmute | ✅ 通过（runAction 通用执行器和 handleFetch/pull/push 均包裹 mute/unmute） |
| const enum 检查（isolatedModules 兼容） | ✅ 通过（git-types.ts 和 text-formatter.ts 均使用普通 enum，非 const enum） |
| Rust 编译验证（cargo build） | ✅ 通过（见下方验证步骤） |
